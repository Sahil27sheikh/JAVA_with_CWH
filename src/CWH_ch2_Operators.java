public class CWH_ch2_Operators {
    public static void main(String[] args) {

              //  Arithmetic Operator ( + , -, *, /, %, ++, -- ) is the arithmetic operators.
         // int a = 5;
         // int b = 10 * a;

         // int a = 5;
         // int b = 23 - a;

         // int a =5;
         // int b = 3 * a; // it is the module operators in arithmetic operators.
         //  System.out.println(b);

             // Assignment Operators ( =, +=, -=, *=, /= )
          int a = 45;
          int b = 5;
          b *= a;
          System.out.println(b);

        // comparison operators ( ==, >=, <=, <, > ) the comparison operators is print the value in true and false.
        // System.out.println(67<68);

        // Logical operators ( && (it is check both are true), || (it check only one and both are true))
        // System.out.println(56<5 && 56>5); // it is print false.


    }
}

// Note: Comparison operators can not work with booleans.
// % operator can also work with float and doubles.

